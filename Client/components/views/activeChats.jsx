import { View, FlatList, Alert, Text, StyleSheet, TouchableOpacity, Image, Platform } from "react-native";

const ActiveChats = () =>{
  return(
    <View>
      <Text>Active chat list</Text>
    </View>
  )
}

export default ActiveChats;